// (exp1) ? true: false


#include<stdio.h>
int main()
{
    int a,b;
    a=5,b=10;
   ( a < b ) ? printf("Its true and the value is %d",a): printf("Its false and the value is %d",b);
    return 0;
}
